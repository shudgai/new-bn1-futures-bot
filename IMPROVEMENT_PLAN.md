# 5m 策略优化改进计划

## 分析摘要
- **分析样本**：259 笔平倉交易
- **整体勝率**：60.62%，但获利能力不足 (Avg PNL -0.0774)
- **获利因子**：0.6956（低于 1.0 盈亏平衡点）
- **主要侵蚀来源**：手续费 (29.1267)、频繁止损 (17.76%)

---

## 问题 1: 极高分值陷阱（Score 95+）

### 现象
- 信号分数 95+ 时勝率仅 33.33%
- 净损益：-19.4023 USDT
- 说明过度强勁的訊号可能与市场极端波动或流动性枯竭相关

### 根本原因
- 极端行情下 ATR 剧烈波动，导致进场后快速止损
- 高分值信号可能在进场时已处于行情耗竭点

### 改进方案
1. **新增高分值 ATR 限制**
   - 对 `readiness_score >= 95` 的信号，添加严格的 ATR_PCT 上限（如 0.003）
   - 防止在波动率过高时盲目进场

2. **新增配置参数**
   ```
   HIGH_SCORE_ATR_LIMIT_PCT = 0.003  # 95+ 分信号的 ATR 上限
   HIGH_SCORE_THRESHOLD = 95          # 触发高分值限制的门槛分
   ```

3. **修改 core/strategy.py**
   - 在进场前检查高分值时的 ATR 限制
   - 高分但 ATR 过高时降分或拒绝进场

---

## 问题 2: 多头交易表现疲软

### 现象
- LONG 交易勝率：53.42%（低于整体 60.62%）
- LONG 止损率：24.66%（显著高于 SHORT）
- Keltner Channel 在多头趋势中的过滤能力较弱

### 根本原因
- 多头进场条件过于宽松
- 缺少对 Keltner Channel 宽度的要求
- 上升趋势中的虚假突破被误认成有效信号

### 改进方案
1. **强化多头 Keltner Channel 宽度要求**
   - 新增参数：`KELTNER_MIN_WIDTH_ATR_MULT_LONG = 1.5`
   - 要求多头进场时 KC 宽度 ≥ 1.5 倍 ATR（比空头更严格）

2. **增强多头的支撑确认**
   - 提高 LONG 时的 Volume Ratio 要求（从 0.30 → 0.40）
   - 提高 LONG 时的 RSI 下限（从 51 → 55）

3. **修改的配置**
   ```
   KELTNER_MIN_WIDTH_ATR_MULT_LONG = 1.5
   SUPPORT_PULLBACK_MIN_VOLUME_RATIO_LONG = 0.40
   SUPPORT_PULLBACK_RSI_LONG_MIN = 55  # 提高多头进场 RSI 门槛
   ```

---

## 问题 3: 手动平倉损耗

### 现象
- 手动平倉净损：-13.4963 USDT
- 手动平倉勝率：53.23%（低于自动止盈的预期）
- 说明人工介入可能在不确定时机导致获利回吐或过早离场

### 根本原因
- 获利目标过于紧湊或过早平倉
- 缺少自动分批止盈逻辑
- 人工判断时机往往不如机械化执行

### 改进方案
1. **启用分批止盈（Partial Take-Profit）**
   ```
   # 新增配置
   ENABLE_PARTIAL_TP = true
   PARTIAL_TP_LEVELS = [0.5, 1.0, 1.5]  # 在 50%, 100%, 150% R 分批止盈
   PARTIAL_TP_RATIOS = [0.33, 0.33, 0.34]  # 各占 1/3 仓位
   ```

2. **自动移动止利（Trailing Stop-Profit）**
   - 进入盈利后，止损向上移动至 breakeven + 0.5 ATR
   - 目标减少因价格回调导致的获利回吐

3. **取消手动平倉**
   - 让系统完全接管出场决策
   - 仅保留手动止损（用户已要求仅允许手动停损）

---

## 改进优先级

| 优先级 | 改进项 | 预期提升 | 实施复杂度 |
|--------|--------|----------|-----------|
| 🔴 高  | 禁用 95+ 分信号或增加 ATR 限制 | 提高高分值勝率 | 低 |
| 🟡 中  | 强化 LONG 交易过滤 | 提高勝率 3~5% | 中 |
| 🟢 低  | 自动化获利目标 | 减少人工损失 | 高 |

---

## 实施步骤

1. **第 1 步**：增加高分值 ATR 限制（配置 + 策略修改）
2. **第 2 步**：强化多头过滤（配置 + 策略修改）
3. **第 3 步**：启用分批止盈（需要在 engine.py 和 account.py 中修改）
4. **第 4 步**：回测验证并发布更新

---

## 验证指标

- 高分值 (95+) 信号勝率 → 目标 ≥ 60%
- LONG 交易勝率 → 目标 ≥ 58%
- 整体获利因子 → 目标 ≥ 1.0
- 手动平倉数量 → 目标减少至 0（完全自动化）
